export const SAMPLE_ARTICLES = [
  {
    id: 'payment-idempotency',
    title: '支付 API：幂等请求与失败恢复',
    content: `# 支付 API：幂等请求与失败恢复

在线支付最难处理的并不是请求失败，而是客户端无法判断请求是否已经成功。网络超时可能发生在服务端完成扣款之后。如果客户端直接重试，就可能造成重复扣款。

> 本接口的核心约束是：同一个业务意图只能产生一笔支付结果，即使客户端重复发送请求。

## 创建支付

客户端通过 \`POST /v1/payments\` 创建支付，并在请求头中提供 \`Idempotency-Key\`。这个键由客户端生成，建议使用 UUID，并在同一笔订单的所有重试中保持不变。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| order_id | 是 | 商户侧唯一订单号 |
| amount | 是 | 以分为单位的正整数 |
| currency | 是 | ISO 4217 货币代码 |
| customer_id | 否 | 商户侧客户标识 |

\`\`\`http
POST /v1/payments HTTP/1.1
Idempotency-Key: 2f7a5a44-6523-4f75-a0c5-1688bd6b5108
Content-Type: application/json

{
  "order_id": "ORDER-2026-0811",
  "amount": 12900,
  "currency": "CNY"
}
\`\`\`

## 幂等规则

1. 幂等键的作用域是当前商户，一个商户不能读取另一个商户的请求结果。
2. 首次请求成功后，服务端保存请求摘要与响应，保留时间为 24 小时。
3. 相同幂等键和相同请求体再次提交时，服务端返回第一次请求的状态与结果。
4. 相同幂等键对应不同请求体时，服务端返回 \`409 IDEMPOTENCY_CONFLICT\`，不会创建新支付。
5. 两个相同请求并发到达时，只有一个请求获得执行权，另一个请求等待或返回 \`409 REQUEST_IN_PROGRESS\`。

## 支付状态

支付创建后可能处于 \`PENDING\`、\`SUCCEEDED\` 或 \`FAILED\`。\`PENDING\` 表示支付渠道仍在处理，不能解释为失败。只有 \`SUCCEEDED\` 是最终成功状态，\`FAILED\` 是最终失败状态。

| 客户端观察 | 推荐动作 | 禁止动作 |
| --- | --- | --- |
| HTTP 连接超时 | 使用原幂等键查询或重试 | 生成新键再次扣款 |
| 409 REQUEST_IN_PROGRESS | 退避后查询支付状态 | 并发持续重试 |
| 5xx 且无响应体 | 使用原幂等键重试 | 直接创建第二笔支付 |
| SUCCEEDED | 保存支付编号并完成订单 | 再次调用创建接口 |

## 失败恢复

客户端应采用指数退避，建议间隔为 1 秒、2 秒、4 秒和 8 秒。超过重试窗口后，客户端应调用 \`GET /v1/payments/by-order/{order_id}\` 查询最终状态。如果查询仍返回 \`PENDING\`，订单应进入人工或异步对账流程，而不是自动判定失败。

结论：幂等键解决的是重复执行问题，状态查询解决的是结果不确定问题。只有两者同时存在，支付客户端才能在网络故障下安全恢复。`,
  },
  {
    id: 'rag-acceptance',
    title: '企业知识库 RAG 上线验收规范',
    content: `# 企业知识库 RAG 上线验收规范

检索增强生成系统的上线判断不能只看回答是否流畅。一个回答可能语言自然，却引用了错误文档；也可能检索到了正确材料，但模型在组织答案时加入了材料中不存在的结论。

## 验收目标

验收需要把系统拆成两个相互依赖的阶段：检索阶段负责找到足够且相关的证据，生成阶段负责严格依据证据回答。检索质量是生成质量的上限，但高质量检索并不自动保证忠实回答。

## 核心指标

| 指标 | 衡量对象 | 建议门槛 |
| --- | --- | --- |
| Recall@5 | 标准证据是否进入前 5 条结果 | 不低于 90% |
| MRR | 第一条正确证据的平均排名 | 不低于 0.8 |
| Faithfulness | 回答中的主张能否被引用材料支持 | 不低于 95% |
| Answer Relevance | 回答是否直接解决用户问题 | 不低于 85% |
| Citation Accuracy | 引用位置是否真的支持相邻结论 | 不低于 95% |

Recall@5 较低通常意味着分块、向量模型、查询改写或权限过滤存在问题。Faithfulness 较低则更可能与提示词约束、上下文噪声或模型能力有关。两个指标必须分开诊断。

## 测试集设计

测试集至少包含以下类别：

1. 事实查询，例如产品价格、版本限制和负责人。
2. 多跳问题，需要组合两份以上文档才能回答。
3. 时效问题，旧制度与新制度同时存在但只有新版本有效。
4. 权限问题，答案存在于知识库中，但当前用户没有访问权限。
5. 不可回答问题，知识库中没有可靠证据。

每道题需要保存标准答案、必要证据、允许的答案变体和不可出现的错误主张。测试题应来自真实搜索日志与业务专家，而不是只由语言模型自动生成。

## 上线门禁

系统只有在离线评测和小流量在线评测都通过后才能全量发布。任何权限越权都属于阻断级问题；关键事实错误率超过 2% 时必须停止发布；不可回答问题的拒答率低于 90% 时，需要继续调整检索阈值与回答策略。

> 没有证据时明确拒答，比给出一个看似完整但无法验证的答案更可靠。

## 监控与回归

上线后应记录查询、检索文档编号、文档版本、模型版本、回答和用户反馈。文档库、嵌入模型、重排器、提示词或生成模型发生变化时，都必须运行同一套回归集。否则团队无法判断质量变化来自哪个环节。

结论：RAG 验收的本质是建立可追踪的证据链。检索指标定位证据是否被找到，忠实度指标判断证据是否被正确使用，权限与拒答测试则控制系统的业务风险。`,
  },
  {
    id: 'fsrs-memory',
    title: 'FSRS 如何安排下一次复习',
    content: `# FSRS 如何安排下一次复习

间隔重复的目标不是让学习者频繁看到卡片，而是在记忆即将变得不可靠之前安排复习。复习太早会浪费时间，复习太晚则会造成大量遗忘。

## 三个记忆状态

FSRS 使用三个变量描述一张卡片的状态：难度、稳定性和可提取性。

- **难度 Difficulty** 表示这张卡片本身有多难学。难度较高的卡片需要更多次成功复习，评分也更容易波动。
- **稳定性 Stability** 表示在当前记忆强度下，记忆保持到一定提取概率所需的时间。稳定性越高，下一次间隔通常越长。
- **可提取性 Retrievability** 表示现在成功回忆的概率。它会随着距离上次复习的时间增加而下降。

可提取性不是固定属性。同一张卡片在刚复习后可提取性很高，数周后可能明显降低。调度器根据经过时间和稳定性估计当前可提取性，再决定是否到期。

## 四档评分

学习者看到答案后，应根据真实回忆情况评分：

| 评分 | 含义 | 对调度的影响 |
| --- | --- | --- |
| 忘记 | 无法正确回忆 | 进入短间隔重新学习，稳定性显著下降 |
| 困难 | 勉强回忆且耗时较长 | 小幅增加稳定性，下次间隔较短 |
| 记得 | 正确回忆且没有明显困难 | 正常增加稳定性 |
| 轻松 | 快速、确定地回忆 | 较大幅度增加稳定性，下次间隔更长 |

评分描述的是回忆表现，而不是学习者希望多久再看到卡片。如果为了获得更长间隔而总是选择“轻松”，模型会高估记忆强度，最终造成集中遗忘。

## 为什么需要个性化

固定间隔方案假设所有人以相同速度遗忘，但真实情况并非如此。熟悉领域的概念容易建立联系，稳定性增长更快；陌生术语缺少既有知识支撑，需要更密集的复习。FSRS 会使用历史评分拟合参数，使预测逐渐接近个人的遗忘规律。

## 好闪卡仍然重要

调度算法无法弥补糟糕的卡片设计。一张卡片如果同时询问多个问题、答案边界模糊或脱离上下文，评分就无法准确反映某个知识点的记忆状态。更可靠的做法是保持问题单一、答案明确，并保存必要的原文依据。

结论：FSRS 的价值不在于把复习间隔简单拉长，而在于根据每张卡片和每个学习者的历史表现估计遗忘风险。稳定性决定记忆能保持多久，可提取性描述当前回忆概率，真实评分则为下一次预测提供数据。`,
  },
];
