# 在 Linux 服务器 / GitHub Actions runner 上执行的脚本必须使用 LF 行尾，
# 否则 CRLF 会导致 `bash: /bin/bash^M: bad interpreter` 之类的解释器错误。
*.sh text eol=lf
scripts/remote-deploy.sh text eol=lf
# CI 工作流 YAML 同样统一 LF，避免 autocrlf 造成的假改动与 runner 端行尾差异
*.yml text eol=lf
*.yaml text eol=lf
