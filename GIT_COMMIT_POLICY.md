# AnchorRead Git 提交规范门禁

本仓库使用 Husky 和 commitlint 校验提交信息，确保提交历史可以按产品能力和功能分组阅读。

## 必须满足

1. 标题符合 `type(scope): 中文摘要`。
2. `type` 为小写英文，并属于允许列表：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`build`、`ci`、`chore`、`revert`、`i18n`。
3. subject 必须包含中文摘要。
4. body 必须使用下列正文格式之一：
   - 有合适分区时，按 `新功能 → 优化 → 修复 → 测试 → 构建 → 其他` 的顺序书写；每个出现的分区至少包含一个编号项（`N. 说明`）。
   - 没有合适分区时，正文直接使用编号列表（可在编号项下使用 `-` 或 `*` 表达子项）。
5. `其他` 只能放在最后，且不能作为唯一分区。

## 推荐格式

```text
feat(reader): 增加文档阅读能力

新功能
1. 新增阅读入口
   - 保留现有文档状态
   - 增加失败提示

测试
1. 更新阅读器和文档测试
```

上述示例只包含本次实际发生的“新功能”和“测试”，且分区顺序正确、每个分区都有编号项。修复、优化或构建类提交应加入对应分区；若没有合适分区，则直接写编号列表。编号前的缩进不作强制要求。

## 本地验证

```powershell
pnpm exec commitlint --edit .tmp\commit-message.txt
git diff --check
```
